/**
 * The features view of the entitlements query. One query backs every plan
 * surface; this module keeps the older import path working.
 */
export {
  useFeature,
  useFeatureGuard,
  useFeatures,
} from "@/hooks/use-entitlements"
